import 'package:flutter/material.dart';

class Saveditems extends StatelessWidget {
  const Saveditems({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("🔖 Your saved items", style: TextStyle(fontSize: 24)),
    );
  }
}
